export enum UserRoles {
  admin = "admin",
  super_admin = "super_admin",
  individual = "individual",
  corporate = "corporate", 
}
export enum individualUserType {
  buyer = "buyer",
  seller = "seller",
}
